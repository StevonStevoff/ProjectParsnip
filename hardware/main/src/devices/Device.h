#ifndef Device_h
#define Device_h

#include "Arduino.h"


class Device {
    public:
        // virtual boolean connectDeviceToWifi(const char* ssid, const char* password) = 0;
    private:
};

#endif